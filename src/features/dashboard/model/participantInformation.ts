export interface ParticipantData {
    id: number;
    orderNumber: number;
    participant: string;
    email: string;
    phoneNumber: string;
    purchaseDate: string;
    ticketName: string;
    orderStatus: string;
    checkedIn: boolean;
}